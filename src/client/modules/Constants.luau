return {
	CANVAS_SIZE = 28,
	PIXEL_SIZE = 12,
	CANVAS_PADDING = 8,
	BRUSH_SIZE = 2,
	PREDICTION_DEBOUNCE = 0.05,
	
	COLORS = {
		bg = Color3.fromRGB(20, 20, 20),
		surface = Color3.fromRGB(30, 30, 30),
		text = Color3.fromRGB(220, 220, 220),
		textMuted = Color3.fromRGB(120, 120, 120),
		danger = Color3.fromRGB(180, 80, 80),
	}
}
